{
  "name": "dev-utils",
  "version": "0.3.1",
  "description": "Basic development utilities",
  "author": {
    "name": "Judi"
  },
  "userConfig": {
    "vault_projects_path": {
      "title": "Vault projects path",
      "description": "Directory where per-project directories are created on session start (e.g. ~/code/vault/projects)",
      "type": "directory",
      "sensitive": false
    }
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/ensure-project-dir.sh"
          }
        ]
      }
    ]
  }
}
